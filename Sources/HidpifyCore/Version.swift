/// hidpify version — the single source of truth shared by the CLI, the menu bar
/// app, and (via Scripts/make-app.sh) the app bundle's Info.plist. Keep this in
/// sync with the git tag / release version.
public enum Hidpify {
    public static let version = "0.1.8"
}
